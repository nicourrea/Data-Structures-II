# Project 1 - Templated Doubly Linked List

Implements `TList<T>` and `TListIterator<T>` with insert/remove front/back, iterators, copy/move, and `operator+`.  
Driver: `mydriver.cpp`.

## Build & Run
make
./templated_list
